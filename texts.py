help_text = """
Interpreter commands:
    ? - get running comp
    d - show current db
    save - save db
    exit, quit, q - save, stop and exit
    stop, cancel, s, c - stop, don't exit
    start, main, m - start computation
    import, i [n, m, ... ]- import to queue; -u to import to unknown. 
    seq [n] - start calculations from number
    track [n] - get last known number of seq
    next [n] - get next number
    force, f [n] - force compute next number
    help - show this menu
    docs - show docs
"""

docs = "No docs yet.."
